# Журнал

Новые записи сверху. Формат: дата — что сделали → что узнали → что дальше.

## 2026-09-26 — Дизайн виджета: Large

- Макеты рисуем в Penpot (через Penpot MCP). Сделали вайрфреймы Large на реальной неделе 21–27.09.
- Раскладка: полоска недели + один раскрытый день с комментарием тренера. Выбрали плотный вариант B:
  самый длинный комментарий (13 строк) влезает целиком.
- Правила: какой день раскрывать, статусы (пропуск — жёлтый, не красный), приоритет доп. информации
  (1 — заметка тренера под комментарием, 2 — следующая тренировка). Понедельник — выходной по умолчанию.
- Документы: [DESIGN.md](02-widget/DESIGN.md) — текущий дизайн, [explorations.md](02-widget/explorations.md) —
  пройденные варианты, [ideas.md](02-widget/ideas.md) — идеи на потом.

- Визуальный стиль: системный, светлая и тёмная тема, **мягкие пастельные** акценты по виду спорта (бег — розовый,
  вело — голубой). Цвет только у случившегося (выполнено, пропуск), тренировки впереди — серые. Цветной фон отбросили.
- Сверили гайдлайны iOS 27: новый размер Extra large (4×6, вся страница), тонированный и прозрачный режимы
  (всё белое — статусы держатся на форме). Нарисовали субботу в этих режимах. Минимальная версия — **iOS 17**.
- Доска в Penpot разложена по секциям: вариант A отдельно, в B — вайрфрейм, сценарии по дням, стили и режимы.

- Особые состояния Large: неделя без плана, не вошёл в аккаунт, две тренировки в день, своя тренировка без плана,
  длинный заголовок.

- Размер шрифта зафиксирован: системная настройка (Dynamic Type) на виджет не влияет.

**Дальше:** Extra large «вся неделя», затем Medium и Small.
Воскресное поздравление — доп, после выхода приложения.

## 2026-09-25 — Примеры данных для дизайна

- Выгрузил неделю 21–27.09 из аккаунта: заголовки, комментарии тренера, выполнена ли тренировка →
  [docs/02-widget/examples/](02-widget/examples/README.md).
- Комментарий тренера лежит в `preActivityComment`; «выполнено» определяем по `resultDataSources` (загрузка из Strava/Coros).
- Репозиторий публичный; владелец решил хранить реальные примеры как есть.

**Дальше:** макеты виджета → `docs/02-widget/mockups/`.

## 2026-09-25 — Эксперимент A: прогон на аккаунте ✅

- Логин и пароль добавлены в переменные окружения (`TE_EMAIL`, `TE_PASSWORD`), в git их нет.
- Вход через нативный поток Kratos работает. **Сессия живёт ~1000 дней** (до 2029-06-21).
- Поправка к разбору: GraphQL принимает сырой токен в `x-session-token`; `Authorization: Bearer` ждёт JWT
  из `whoami?tokenize_as=long_lived_token` (30 дней). Прототип переведён на `x-session-token`.
- `calendarItems` на бесплатном аккаунте отдаёт тренировки, план/факт, структуру. Прототип печатает неделю.
- Google Calendar: интеграция `connected`, но ни одна тренировка туда не выгружалась (нет записи в `syncWithProviders`).
- Решение этапа 1: **основной способ — A** ([DECISION.md](01-data-source/DECISION.md)).

**Дальше:** этап 2 — модель `Workout` для виджета и где живёт логика (скорее всего прямо в iOS-приложении).

## 2026-09-25 — Эксперимент A: разбор веб-приложения

- Сетевой доступ к `trainingendurance.com` и `app.trainingendurance.com` заработал после повторной настройки
  окружения (Network access → Custom, домены `trainingendurance.com` и `*.trainingendurance.com`).
- Разобрал SPA (React + Apollo, ~110 JS-чанков). API: **GraphQL** `app.trainingendurance.com/graphql`,
  интроспекция открыта, данные без токена не отдаются.
- Вход: **Ory Kratos**, есть нативный API-поток (`/kratos/self-service/login/api`) — логин/пароль → `session_token`
  без cookies и CSRF. Способы входа: пароль, код на почту, Google, Apple. (Здесь я ошибочно решил, что токен
  идёт в `Authorization: Bearer` — на деле в `x-session-token`, см. запись выше.)
- Календарь: `calendarItems(p: {userID, dates: [...]})` → тренировки с `name`, `workoutType`, `plan`/`fact`
  (длительность, дистанция, ESS), `manualDone`, структура.
- Вариант B: iCal нет, но есть интеграция **Google Calendar** («все запланированные тренировки выгружаются»).
- Нашёл официальный **OAuth 2.1 + MCP-сервер** (`mcp.trainingendurance.com`), но он только для PRO → вариант J в options.
- Написал прототип [`tools/te-probe/te_probe.py`](../tools/te-probe/te_probe.py), проверил запросы на схеме.

**Дальше:** логин/пароль TE в `secrets/te.env` → прогон прототипа, срок жизни сессии. Параллельно:
включить Google Calendar в настройках TE и посмотреть, что приходит.

## 2026-09-25 — Сетевой доступ

- Владелец добавил `trainingendurance.com` и `app.trainingendurance.com` в разрешённые домены окружения.
  В текущем контейнере это не сработало: прокси по-прежнему отвечает 403 на оба домена. Скорее всего,
  настройки применяются при старте нового контейнера — проверяем в новой сессии.
- Документы перенесены в ветку `main`.

**Дальше:** в новой сессии проверить доступ (`curl -I https://app.trainingendurance.com`) → эксперимент A.
Если доступа нет — HAR из браузера владельца.

## 2026-09-25 — Старт, разведка

- Сделал структуру документов (`README`, `PLAN`, `PROGRESS`, `QUESTIONS`, `docs/01-data-source/`).
- Нашёл сервис: Training Endurance (trainingendurance.com, разработчик Artem Smirnov). Есть веб-приложение
  `app.trainingendurance.com`, приложения для iOS и Android. Заявлены интеграции: импорт выполненных
  тренировок из Garmin/Polar/Wahoo/Suunto/Strava, экспорт тренировок в Garmin/Polar/Wahoo/Suunto/Apple Watch.
- Публичного API, iCal-экспорта или документации для разработчиков поиск не нашёл. Это не значит, что их нет:
  нужно проверить руками в настройках.
- Собрал 9 вариантов получения данных → [options.md](01-data-source/options.md).
- **Блокер:** из облачного окружения, где я работаю, закрыт сетевой доступ к `trainingendurance.com`,
  `app.trainingendurance.com`, `apps.apple.com`, `play.google.com`. Сам открыть веб-приложение и посмотреть
  его запросы я пока не могу. Варианты: открыть эти домены в настройках окружения или снять запросы
  в браузере самому (инструкция в [эксперименте A](01-data-source/experiments/A-private-web-api.md)).

**Дальше:** ответы на [вопросы](QUESTIONS.md) → проверки B, A, G.
